package cihan.kurs.odev11.javaLambda;

public class MyMath {
	public static boolean checker(PerformOperation p, int num) {
		return p.check(num);
	}
	
	public static boolean isOdd(PerformOperation p, int num) {
		return p.check(num);
	}
	public static boolean isPrime(PerformOperation p, int num) {
		return p.check(num);
	}
	public static boolean isPalindrome(PerformOperation p, int num) {
		return p.check(num);
	}
}
